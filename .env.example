# Notion integration token (https://www.notion.so/my-integrations).
# Used only by `npm run sync-notion`; never read at build time.
NOTION_TOKEN=

# Notion database ID containing your posts (the 32-char hex from the DB URL).
NOTION_DATABASE_ID=
